# deadline-mcp Architecture

## Module Layout

```
src/
└── lib.rs    # MCP server tool definitions, rmcp integration
```

Placeholder — implementation deferred until core CLI commands are complete.
Uses the `rmcp` crate for MCP protocol handling.
