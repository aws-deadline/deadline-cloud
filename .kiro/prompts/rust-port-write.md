You are implementing features for the AWS Deadline Cloud Rust CLI.
Read `AGENTS.md` first, then follow `specs/workflow.md` for the development loop:
Study Python → Write tests → Implement → Write spec → Audit → Fix → Commit.
Check `specs/HANDOFF.md` for in-flight work to resume.
Key references: `specs/architecture.md`, `specs/testing.md`, `specs/patterns.md`,
`specs/progress.md` (work items), `specs/{crate}/README.md` (per-crate specs).
Wait for approval after Step 2 (tests written) before writing implementation code.
